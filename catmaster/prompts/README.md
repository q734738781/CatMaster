# CatMaster Prompt Assets

Package-internal prompt assets live here. They are source assets for future
prompt rendering and parity migration work; they must not be copied to
workspace `files/.deepagents/`, DeepAgents memory, or skill roots.

The active specialist runtime still uses the existing hard-coded prompt
builders until a parity-tested migration is performed.
