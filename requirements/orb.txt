# Isolated remote ORB-v3 execution environment. orb-models requires Python 3.12+.
orb-models==0.7.0
torch==2.8.0
ase==3.29.0
numpy==2.2.6
scipy==1.17.1
sella==2.5.0
jax==0.7.2
jaxlib==0.7.2
pyyaml==6.0.3
# orb-models installs PySCF for molecular support; pin its required HDF5 binding
# so pip check remains clean in the isolated provider environment.
h5py==3.16.0
